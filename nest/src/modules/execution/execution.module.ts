import { Module } from '@nestjs/common';

/**
 * ExecutionModule — DDD stub. Futuro:
 *   - GenerateReportUseCase (HTML/PDF + MinIO upload)
 *   - SendReportEmailUseCase
 */
@Module({})
export class ExecutionModule {}
