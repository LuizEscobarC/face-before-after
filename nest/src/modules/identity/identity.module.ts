import { Module } from '@nestjs/common';

/**
 * IdentityModule — DDD stub. Futuro:
 *   - SessionAggregate (anonymous/authenticated)
 *   - SessionRepository
 *   - JWT guards
 */
@Module({})
export class IdentityModule {}
