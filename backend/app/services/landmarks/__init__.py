"""Virtual landmark extraction and fusion services.

Modules:
  virtual_landmarks  — extract hairline points from a BiSeNet hair mask.
  fusion_layer       — fuse MediaPipe landmarks with BiSeNet segmentation.
"""
