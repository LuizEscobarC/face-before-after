---
tenant_id: "face-before-after"
project: "face-before-after"
module: "plans/pr53b-pr54-30-metrics-2026-05-11"
file_path: ".claude/plans/pr53b-pr54-30-metrics-2026-05-11.md"
doc_type: "decision"
created_at: "2026-05-24"
updated_at: "2026-05-24"
version: "1.0.0"
summary_context: >
  Data: 2026-05-11 Origem: chat — usuário pediu 'planeje e execute EM PARALELO OS DOIS PR-53b ... PR-54 ... já está com opus 4.7' Status: Aprovado pelo usuário; janela imediata = Fases A + B; Fase C requer prompts separados por sub-onda.
tags:
  - "planning"
  - "metrics"
rag_keywords:
  - "metrics"
  - "plans"
  - "pr53b"
  - "pr54"
related_modules: []
depends_on: []
used_by: []
---
# Plan: PR-53b + PR-54 + 30 Novas Métricas (M4 + M2-Extension)

**Data:** 2026-05-11
**Origem:** chat — usuário pediu "planeje e execute EM PARALELO OS DOIS PR-53b ... PR-54 ... já está com opus 4.7"
**Status:** Aprovado pelo usuário; janela imediata = Fases A + B; Fase C requer prompts separados por sub-onda.

---

## TL;DR

Três streams. **(A) PR-53b** → 336 templates short+long para os 56 metric_id já cobertos × 3 severidades, em 1 migration TypeORM idempotente. **(B) PR-54** → auto-revisão de tom + blacklist v0.2 (~15 termos novos) + relatório markdown para revisão humana posterior. **(C) PR-NEW (M2-Extension)** → 30 métricas novas end-to-end (Python calculator + ideal + peso + testes + 9 templates cada) em 3 sub-ondas paralelas, ~1 sub-onda/semana, padrão M2 family-PR.

A presente prompt cobre **somente Fases A + B**. Fase C ganha 3 prompts separados quando A + B fecharem.

---

## Decisões já travadas

- **Escopo das não-cobertas**: opção C — implementar 30 metric_definitions reais com calculator Python.
- **PR-54**: opção A — Opus auto-revisa + estende blacklist + gera relatório.
- **Forma de entrega**: migration TS pura (descarta `scripts/seed_diagnostic_templates_short_long.py`).
- **Sizes**: short ≤120 char, medium ≤350 char, long parágrafo ≤500 char (DEC-31).
- **Severidades**: mild/moderate/strong (severity_collapse_policy 5→3).
- **Locale**: pt-BR only (DEC-30).
- **Versão**: tudo entra como `version='v1.0'` ativo (additive).

---

## Fase A — PR-53b (336 templates short+long)

**Output**: `nest/src/database/migrations/1746000250000-M42bDiagnosticTemplatesShortLong.ts`.

1. Carregar via SQL os 168 templates existentes (M42, size=medium) agrupados por `(metric_id, severity, direction)`.
2. Para cada combinação, gerar **par short+long** preservando vocabulário do medium correspondente:
   - **short** ≤120 char, foco no fato observado, máx 1 placeholder.
   - **long** parágrafo ~300–500 char, pode usar todos os 5 placeholders permitidos.
   - Verbos da linha vermelha: só `observa-se | indica | apresenta | tende a apresentar`. Severity=strong nunca usa "severo" → "considerável".
   - Cada template precisa passar 4 gates do `TemplateRendererService`.
3. **Paralelização**: 3 subagentes Opus, um por grupo:
   - **A.1** symmetry+thirds+fifths (5+3+6=14 métricas × 3 sevs × 2 sizes = 84 templates)
   - **A.2** eyes+brows+nose+mouth (6+6+7+7=26 × 3 × 2 = 156)
   - **A.3** jaw+cheekbones+forehead+global (6+5+3+2=16 × 3 × 2 = 96)
4. Main agent agrega no array `SHORT_LONG_TEMPLATES`. INSERT idempotente com `ON CONFLICT (version, metric_id, severity, direction, size) DO NOTHING`. `placeholders_used` JSONB derivado por regex.
5. `npm run typeorm:run` + `npm run lint:templates` + `npm test` + `npx tsc --noEmit`.

**Critério de pronto A**: 336 novas linhas em `diagnostic_template`; lint exit 0; 201/201+ Vitest verdes; tsc clean.

---

## Fase B — PR-54 (auto-revisão + blacklist v0.2)

**Output 1**: `nest/src/database/migrations/1746000260000-M42cTemplateBlacklistExtensionV02.ts` — sobe blacklist para v0.2 + 15 termos novos (`severo, gravíssimo, alarmante, comprometido, fraco, ruim, irreversível, drástico, garantido, transformador, milagre, único caminho, definitivo, preocupante, deficitário`).

**Output 2**: `.claude/plans/marcos/M4_PR54_TONE_REVIEW.md` — relatório markdown agrupado por região/severidade. Cada template recebe scores de "vendedorismo" / "pejorativo" / "mensurabilidade" + sugestão de rewrite quando algum score estiver alto.

1. Auditar 168 mediums existentes com heurística estendida (paralelo com A.2).
2. Rodar `lint-templates.ts` v0.2 contra mediums + 336 short+long da Fase A → captura hits.
3. Para cada hit ou template com score alto, Opus propõe rewrite no markdown. Marca `STATUS: needs_review | safe | rewrite_proposed`.
4. Migration `M42c` ativa v0.2 (deactiva v0.1, insere termos novos).
5. **Deferido a humano**: aprovar markdown → migration follow-up `M42d` aplica os UPDATE statements.

**Critério de pronto B**: v0.2 ativa no DB; lint passa; markdown gerado em `.claude/plans/marcos/`.

---

## Fase C — 30 métricas novas (3 sub-ondas, semanas, prompts separados)

Lista canônica das 30 (a revisar com usuário antes de C1):

C1: brow_tail_drop_r, hairline_curvature_index, e_line_deviation, face_shape_classification, scleral_show_lower_l/r, palpebral_fissure_inclination, supratarsal_fold_visibility, nasal_dorsum_straightness, columella_show.
C2: nasolabial_angle_proxy, alar_flare_index, cupids_bow_definition, lip_volume_ratio, oral_commissure_height_asym, philtrum_width_ratio, smile_line_curvature, chin_projection_proxy, mandibular_corpus_length_ratio, masseteric_prominence_proxy.
C3: mentolabial_fold_proxy, brow_arch_peak_position_l/r, intersuperciliary_distance_ratio, buccal_fat_index, ogee_curve_proxy, infraorbital_hollow_index, forehead_slope_proxy, glabella_prominence_proxy, facial_index_anthropometric.

Padrão por sub-onda: calculator Python → registry → pytest → migration `SeedNewMetricsWaveCN.ts` (definition + ideal + region_weight v1.5) → 9 templates cada.

---

## Ordem de execução

```
Fase 0 (baseline ~30min)
   ├─ Fase A ┐
   │         ├─ paralelo
   ├─ Fase B ┘
   │
   └─ Fase C1 → C2 → C3 (prompts separados, semanas)
```

PR-53b + PR-54 ≈ 1–2 dias concorrentes. 30 métricas ≈ 3–5 semanas.

---

## Arquivos relevantes

- `nest/src/database/migrations/1746000220000-M42DiagnosticTemplatesV1.ts` — pattern de seed (placeholders_used derivado, ON CONFLICT DO NOTHING).
- `nest/src/database/migrations/1746000180000-M41DiagnosticTemplates.ts` — schema (4 tabelas, blacklist v0.1).
- `nest/src/modules/diagnosis/template-renderer.service.ts` — `TemplateRendererService.render` + `ALLOWED_PLACEHOLDERS`.
- `nest/scripts/lint-templates.ts` — `npm run lint:templates`.
- `nest/src/database/migrations/1746000060000-SeedJawFamily.ts` — pattern canônico para Fase C.
- `backend/app/domain/face_metrics.py` — onde calculators novos entram na Fase C.
- `.claude/plans/PLAN_METRICS.md` — plano mestre.
- `.claude/plans/DDL_REVIEW.md` — referência de tabelas.
- `.claude/plans/marcos/M4_NARRATIVE.md` — DEC-30..DEC-41 e linha vermelha §1.1.

---

## Verificação

1. `cd nest && npm run typeorm:run` aplica sem erro.
2. `cd nest && npm run lint:templates` exit 0.
3. `cd nest && npm test` 201/201+ verde.
4. `cd nest && npx tsc --noEmit` clean.
5. `SELECT severity, size, COUNT(*) FROM diagnostic_template WHERE version='v1.0' GROUP BY 1,2;` → 168 medium / ~168 short / ~168 long.
6. `.claude/plans/marcos/M4_PR54_TONE_REVIEW.md` existe com todas as linhas categorizadas.

---

## Considerações abertas (decididas em chat)

1. NarrativeService consome só medium hoje; short→cards (deferido), long→PDF (PR-67). Templates curto/longo ficam dormentes até PR-67 — investimento aceito.
2. Lista canônica das 30 a confirmar antes de C1.
3. Pesos novos entram em v1.5 com weight=0.1 provisional.
4. PR-56b (aesthetic_procedure) fora deste plano.

---

## Recommended Execution Model

- **Model:** opus
- **Reason:** Authoring de 336 templates exige judgment de tom (linha vermelha §1.1: verbos permitidos, severidade=strong sem "severo"), conformidade com 4 gates do `TemplateRendererService` + blacklist + DEC-31 char limits + DEC-30 locale + preservação semântica do vocabulário dos 168 mediums existentes. PR-54 exige scoring qualitativo de "vendedorismo / pejorativo / mensurabilidade" e propostas de rewrite — tarefa de Opus por definição.




# Plan: PR-53b + PR-54 + 30 Novas Métricas (M4 + M2-extension)

> **STATUS 2026-05-11 — PHASE A+B COMPLETE.** M42b applied (336 short+long → 504 total v1.0 rows = 56 metrics × 3 sev × 3 size). M42c applied (blacklist v0.2 active = 25 terms; v0.1 deactivated = 10). Gates: migration:run ✓ | lint:templates ✓ (504 rows, 0 hits) | tsc ✓ | vitest 201/201 ✓. Audit report at `.claude/plans/marcos/M4_PR54_TONE_REVIEW.md`. Phase C (30 new metrics, sub-waves C1/C2/C3) remains DEFERRED — see this plan.



**TL;DR**: Três streams em paralelo. (A) PR-53b: 336 templates short+long para os 56 metric_id já cobertos com `severity ∈ {mild,moderate,strong}` (+ extensão para os 4 stubs scoreáveis: `brow_tail_drop_r`, `hairline_curvature_index`, `e_line_deviation`, `face_shape_classification` se virar scoreável) — 1 migration TypeORM idempotente. (B) PR-54: auto-revisão de tom feita pelo Opus + extensão da blacklist (~15 termos novos sobre PR-50 v0.1) + relatório markdown de templates suspeitos para revisão humana posterior. (C) **PR-NEW (M2-Extension)**: implementar 30 métricas novas end-to-end em 3 sub-ondas paralelas de ~10 métricas cada (escopo confirmado pelo usuário — semanas, não dias). Cada sub-onda = 1 família-PR estilo M2 (Python calculator + ideal + region weight + Vitest+pytest + 9 templates por métrica).

A escolha de implementar 30 métricas novas foi confirmada explicitamente pelo usuário ciente do custo.

---

## Decisões já travadas (do diálogo)

- **Escopo das não-cobertas**: opção C — criar 30 metric_definitions NOVAS de verdade, com calculator Python + ideal + peso regional + testes. Não reaproveita stubs antigos como atalho.
- **PR-54**: opção A — Opus faz auto-revisão + estende blacklist + gera relatório. Revisão humana final fica para o usuário aprovar o relatório, não bloqueia.
- **Forma de entrega**: opção A — uma migration TS por entrega (padrão do projeto). Sem script Python paralelo. Idempotência via `ON CONFLICT DO NOTHING`.
- **Sizes**: short ≤120 char, medium ≤350 char, long parágrafo completo (≤500 char) — DEC-31.
- **Severidades**: continua só mild/moderate/strong (severity_collapse_policy 5→3). Extreme + minimal ficam para PR-53c (deferido).
- **Locale**: pt-BR only (DEC-30).
- **Versão**: tudo entra como `version='v1.0'` ativo (additive — não faz version bump).

---

## Fases e paralelismo

### Fase 0 — Baseline e descoberta (sequencial, primeiro)

1. Confirmar 201/201 Vitest + tsc clean ainda valem; rodar `npm run lint:templates` no estado atual.
2. Query no `metric_definition` (real, não a estimativa): listar exatamente quais metric_id têm `presentation_only=true` ou `requires_pixel_analysis=true` (= não vão para score) — isso define quem está "scoreável-mas-sem-template".
3. Definir a **lista canônica de 30 novas métricas** (recomendada abaixo, baseada em Naini 2011 + Farkas 1994 + literatura citada nas migrations existentes), revisar com o usuário antes de Fase C arrancar.

### Fase A — PR-53b: Templates short+long (independente, pode iniciar imediatamente)

**Output**: `nest/src/database/migrations/1746000250000-M42bDiagnosticTemplatesShortLong.ts` (~336 templates).

A1. Carregar via SQL os 168 templates existentes (M42, size=medium) agrupados por `(metric_id, severity, direction)`. *paralelo com B1*

A2. Para cada combinação, gerar **par short+long** preservando o vocabulário do medium correspondente:
   - **short** ≤120 char, foco no fato observado, máx 1 placeholder.
   - **long** parágrafo (~300–500 char), pode usar todos os 5 placeholders permitidos (`value`, `ideal`, `deviation_pct`, `direction_label`, `region_pt`), incluir contexto anatômico breve.
   - Verbos da linha vermelha (§1.1): só `observa-se | indica | apresenta | tende a apresentar`. Severity=strong proibido usar "severo" → "considerável".
   - Cada template precisa passar 4 gates do `TemplateRendererService` (allowlist, no-undeclared, no-missing, no-extraneous).

A3. **Paralelização interna**: lançar 3 subagentes Opus em paralelo, um por grupo de família:
   - Agent A3.1: `symmetry` + `proportion (thirds/fifths)` (5+3+6=14 métricas × 3 sevs × 2 sizes = 84 templates)
   - Agent A3.2: `eyes` + `brows` + `nose` + `mouth` (6+6+7+7=26 × 3 × 2 = 156 templates)
   - Agent A3.3: `jaw` + `cheekbones` + `forehead` + `global` (6+5+3+2=16 × 3 × 2 = 96 templates)
   - Total: 336 templates

A4. Main agent agrega os outputs e monta o array `SHORT_LONG_TEMPLATES` na migration. INSERT com `ON CONFLICT (version, metric_id, severity, direction, size) DO NOTHING` (idempotente). `placeholders_used` JSONB derivado por regex sobre `template_pt`.

A5. Rodar `npm run typeorm:run`, `npm run lint:templates`, `npm run test`, `npx tsc --noEmit`.

**Critério de pronto A**: 336 novas linhas em `diagnostic_template`, 0 hits no lint, 201/201 Vitest, tsc clean.

### Fase B — PR-54: Auto-revisão de tom + blacklist estendida (paralelo com A, depois consome A)

**Output 1**: `nest/src/database/migrations/1746000260000-M42cTemplateBlacklistExtensionV02.ts` — sobe `template_blacklist_version` para `v0.2 (active)` e adiciona ~15 termos novos.

**Output 2**: `.claude/plans/marcos/M4_PR54_TONE_REVIEW.md` — relatório markdown agrupado por região/severidade, com cada template highlightado por:
   - score de "vendedorismo" (presença de superlativos, exclamações, promessas);
   - score de "pejorativo" (presença de termos clínicos negativos não pegos pela blacklist);
   - score de "mensurabilidade" (cita ou não o número/ideal — long deve, short pode pular);
   - sugestão alternativa do Opus quando algum score estiver alto.

B1. Auditar 168 mediums existentes contra heurística estendida (lista abaixo). *paralelo com A2*
   - Termos sugeridos para v0.2: `severo, severa, gravíssimo, alarmante, preocupante, comprometido, fraca, fraco, ruim, péssimo, deficitário, insuficiência, irreversível, drástico, drástica`.
   - Mais 5 termos de "vendedorismo" para banir: `garantido, garantida, transformador, milagre, único caminho, definitivo`.

B2. Rodar `lint-templates.ts` com a v0.2 contra os 168 mediums + 336 short+long da Fase A → captura hits.

B3. Para cada hit ou template com score alto, Opus propõe rewrite no markdown de revisão. Marca `STATUS: needs_review | safe | rewrite_proposed`.

B4. Migration `M42c` faz `INSERT INTO template_blacklist_version (version='v0.2', is_active=TRUE)` + `UPDATE` desativando v0.1 + `INSERT` dos 15 termos novos.

B5. Após usuário aprovar o markdown (passo manual fora do agente), uma migration follow-up `M42d` aplicaria os UPDATE statements escopados por `(version, metric_id, severity, direction, size)`. **Esta última é deferida** — o agente para no markdown.

**Critério de pronto B**: v0.2 ativa no DB, lint passa contra novos termos, markdown gerado em `.claude/plans/marcos/`.

### Fase C — 30 métricas novas (3 sub-ondas paralelas, semanas)

Cada sub-onda = padrão M2 family-PR (referência: PR-13 jaw, PR-14 nose, PR-16 brows). Cada métrica nova exige: (1) calculator Python em `backend/app/domain/face_metrics.py` ou novo módulo de família, (2) registry em `backend/app/services/normalization/metric_registry.py`, (3) testes pytest (`backend/tests/`), (4) row em `metric_definition` + `metric_ideal` + `region_metric_weight` via migration TS, (5) 9 templates (3 sevs × 3 sizes) na migration de templates.

**Lista canônica recomendada (revisar com usuário antes de iniciar)**:

| # | metric_id | região | rationale |
|---|-----------|--------|-----------|
| 1 | brow_tail_drop_r | brows | completar par L/R (PR-16 só fez L) |
| 2 | hairline_curvature_index | forehead | promover stub PR-18 a calculador real |
| 3 | e_line_deviation | global | promover stub PR-19 (Ricketts E-line proxy 2D) |
| 4 | face_shape_classification | global | promover de presentation_only para scoreável |
| 5 | scleral_show_lower_l | eyes | exposição esclerótica inferior — Naini §5 |
| 6 | scleral_show_lower_r | eyes | par L/R |
| 7 | palpebral_fissure_inclination | eyes | ângulo da fenda palpebral |
| 8 | supratarsal_fold_visibility | eyes | sulco palpebral |
| 9 | nasal_dorsum_straightness | nose | desvio do dorso ao midline |
| 10 | columella_show | nose | Naini §6 |
| 11 | nasolabial_angle_proxy | nose | proxy 2D do ângulo nasolabial |
| 12 | alar_flare_index | nose | abertura alar relativa |
| 13 | cupids_bow_definition | mouth | revivido do PR-15 |
| 14 | lip_volume_ratio | mouth | upper+lower vs face_height |
| 15 | oral_commissure_height_asym | mouth | assimetria de canto da boca |
| 16 | philtrum_width_ratio | mouth | revivido do PR-15 |
| 17 | smile_line_curvature | mouth | curvatura da linha labial |
| 18 | chin_projection_proxy | jaw | menton vs subnasale (2D proxy) |
| 19 | mandibular_corpus_length_ratio | jaw | gônio→menton vs bizigomático |
| 20 | masseteric_prominence_proxy | jaw | abaulamento masseteriano (proxy de borda) |
| 21 | mentolabial_fold_proxy | jaw | sulco mentolabial |
| 22 | brow_arch_peak_position_l | brows | posição horizontal do pico (0..1) |
| 23 | brow_arch_peak_position_r | brows | par L/R |
| 24 | intersuperciliary_distance_ratio | brows | distância intersobrancelhar |
| 25 | buccal_fat_index | cheekbones | abaulamento bucal |
| 26 | ogee_curve_proxy | cheekbones | curva Ogee 2D proxy |
| 27 | infraorbital_hollow_index | cheekbones | tear-trough proxy |
| 28 | forehead_slope_proxy | forehead | inclinação frontal 2D |
| 29 | glabella_prominence_proxy | forehead | proeminência glabelar 2D |
| 30 | facial_index_anthropometric | global | índice facial Farkas (face_h/face_w × 100) |

**Sub-onda C1** (paralelo entre si, sequencial dentro): métricas 1–10. Stream-leader: backend Python.
**Sub-onda C2**: métricas 11–20. Pode iniciar quando C1 entregar a primeira migration (para reusar pattern).
**Sub-onda C3**: métricas 21–30. Idem.

C-pattern (cada sub-onda):
   1. Adicionar `MetricCalculator` para cada métrica em `backend/app/domain/face_metrics.py` (ou módulo separado por família).
   2. Registrar em `metric_registry.py`.
   3. Escrever 5–10 pytest unitários por métrica (fixtures `perfect_thirds`, `asymmetric_jaw`, etc.).
   4. Criar migration `1746000270000-SeedNewMetricsWaveC1.ts` (e C2/C3) seguindo padrão `SeedJawFamily`: insere `metric_definition` + `metric_ideal` + `region_metric_weight v1.5`.
   5. Atualizar YAMLs `region_metric_weights.v1_5.yaml` e `global_weights.v1_5.yaml` se necessário.
   6. Adicionar 9 templates por métrica (3 sevs × 3 sizes) à migration `M42b` (Fase A) ou criar `M42b-extension`.
   7. Rodar pytest + Vitest + lint:templates + tsc.

**Critério de pronto C**: 30 novas linhas em `metric_definition`, ~25–28 em `metric_ideal` (presentation_only podem ficar sem ideal), pytest verde com ~150–300 testes novos, 270 templates novos no DB, lint clean.

---

## Ordem de execução recomendada

```
Fase 0 (sequencial, ~2h)
   ├─ A1 ┐
   │     ├─ A2 (3 subagentes paralelos) → A4 → A5  [PR-53b pronto]
   │     │
   ├─ B1 ┘
   │     └─ B2 → B3 (consome A4) → B4  [PR-54 pronto]
   │
   └─ Fase 0 confirma escopo C com usuário
        └─ C1 (paralelo com C2/C3 após pattern estável)
             └─ C2
                  └─ C3  [30 métricas prontas em N semanas]
```

Tempo estimado: PR-53b + PR-54 ≈ 1–2 dias (concorrentes). 30 métricas novas ≈ 3–5 semanas (uma sub-onda por semana, estilo M2).

---

## Arquivos relevantes

- `nest/src/database/migrations/1746000220000-M42DiagnosticTemplatesV1.ts` — referência de pattern de seed (168 mediums, `placeholders_used` derivado).
- `nest/src/database/migrations/1746000180000-M41DiagnosticTemplates.ts` — schema base (4 tabelas, blacklist v0.1).
- `nest/src/modules/diagnosis/template-renderer.service.ts` — `TemplateRendererService.render()` + `ALLOWED_PLACEHOLDERS` + 4 erros tipados.
- `nest/scripts/lint-templates.ts` — `npm run lint:templates`, exit 0/1/2.
- `nest/src/database/migrations/1746000060000-SeedJawFamily.ts` — pattern canônico de family-PR (metric_definition + metric_ideal + region_metric_weight em uma migration).
- `nest/src/database/migrations/1746000150000-RebalanceWeightsV15.ts` — referência para adicionar pesos novos a v1.5 sem mexer em v1.0.
- `backend/app/domain/face_metrics.py` — onde calculators novos entram (ou módulos separados se a família crescer).
- `backend/app/services/normalization/metric_registry.py` — registro de calculators.
- `scripts/seed_diagnostic_templates_short_long.py` — **descartar** (decisão do usuário: migration TS, não Python).
- `.claude/plans/PLAN_METRICS.md` — plano mestre (releitura obrigatória antes de C).
- `.claude/plans/DDL_REVIEW.md` — referência das tabelas de catálogo/avaliação.
- `.claude/plans/marcos/M4_NARRATIVE.md` — DEC-30..DEC-41 e linha vermelha §1.1.

---

## Verificação

1. `cd nest && npm run typeorm:run` — sem erro, migrations aplicam.
2. `cd nest && npm run lint:templates` — exit 0, zero hits.
3. `cd nest && npm test` — 201/201+ Vitest verdes (mais os novos testes de templates se houver).
4. `cd nest && npx tsc --noEmit` — clean.
5. SQL spot-check: `SELECT severity, size, COUNT(*) FROM diagnostic_template WHERE version='v1.0' GROUP BY 1,2 ORDER BY 1,2;` → distribuição esperada (medium 168, short ~168, long ~168 + novas).
6. `cd backend && pytest` — ~1100+ verdes (após Fase C).
7. Smoke: rodar `POST /v1/analysis/evaluate` em foto de fixture e checar que `diagnostic_text` aparece para todas as métricas avaliadas (NarrativeService consome medium hoje; short/long ficam para PDF/cards).
8. Markdown de PR-54 existe em `.claude/plans/marcos/M4_PR54_TONE_REVIEW.md` com todas as 168+336+ linhas categorizadas.

---

## Considerações abertas

1. **NarrativeService consome só medium hoje** — quem consome short/long? PDF (PR-67) + frontend cards (deferido). Se nenhum consumidor existe ainda, short+long ficam dormentes no DB. Recomendação: documentar no plano e seguir; vale o investimento porque PR-67 já está mapeado.
2. **Ideal vs presentation_only** para as 30 novas: algumas (face_shape_classification, índices antropométricos) podem entrar como `presentation_only=true`, sem ideal nem peso. Decidir caso a caso na sub-onda.
3. **Pesos v1.5 vs v2.0**: 30 métricas novas mudam o universo de pesos regionais. Recomendação: adicioná-las em v1.5 com `weight=0.1` provisional; promoção a v2.0 fica para o PR-22 de calibração com fotos reais.
4. **Modelo Opus 4.7 para Fase C**: usuário disse "já está com opus 4.7". Confirmar antes de iniciar cada sub-onda (custo).
5. **`brow_thickness_l/r` + 4 phi/golden permanecem `presentation_only`** (DEC-6) — fora das 30 novas.
